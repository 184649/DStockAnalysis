# DStockAnalysis 結合試験項目書

| 項目 | 内容 |
|---|---|
| 対象 | ViewModel を起点とした画面横断フロー（データ取込→スクリーニング→個別分析→比較） |
| 試験方式 | 自動（xUnit、`Tests/Integration`）＋ 手動確認（画面表示） |
| 実行コマンド | `dotnet test Tests/DStockAnalysis.Tests.csproj` |
| 実施日 | 2026/06/21 |
| 結果 | 自動 12 ケース合格・0 失敗（総合 77 合格）／手動確認 OK |

各ケースの ID はテストコードのコメント（例：IT-MV-01）と一致する。

---

## 1. サンプルデータ連携（SampleDataIntegrationTests.cs）

| ID | 試験項目 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|
| IT-SD-01 | 生成と時系列 | CreateSampleStocks | 10 銘柄以上・各 History 10 年 | 合格 |
| IT-SD-02 | スコア算出 | 生成銘柄を検証 | バフェット/総合/長期が 0–100 | 合格 |
| IT-SD-03 | 総合利回り整合 | 優待銘柄を検証 | 総合=配当+優待 | 合格 |
| IT-SD-04 | 再計算の冪等性 | Recalculate を2回 | 値が不変 | 合格 |

## 2. 画面横断フロー（MainViewModelIntegrationTests.cs）

| ID | 試験項目 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|
| IT-MV-01 | 起動 | MainViewModel 生成 | 全銘柄(JPX)読込・初期=スクリーニング・全件表示 | 合格 |
| IT-MV-02 | 画面切替 | 各 ShowXxxCommand 実行 | CurrentViewModel が切替 | 合格 |
| IT-MV-03 | 市場トグル絞込 | MarketPR=true | 一覧がプライムのみ | 合格 |
| IT-MV-04 | プリセット適用 | 「株主優待あり高利回り銘柄」適用 | 一覧が優待ありのみ | 合格 |
| IT-MV-05 | 個別分析遷移 | OpenAnalysis(stock) | 選択反映・時系列の遅延生成・チャート/レーダー6/チェック15 構築 | 合格 |
| IT-MV-06 | チェック→再計算 | 全いいえ→全はい | バフェットスコア上昇・範囲内 | 合格 |
| IT-MV-07 | 比較追加 | Add 2 銘柄＋重複 Add | 比較対象登録・グラフ構築・重複無視 | 合格 |
| IT-MV-08 | 比較削除 | Remove | 比較対象から除外 | 合格 |

---

## 3. 手動確認項目（画面表示・操作）

実機（Windows）で `run.bat` 起動後に確認。

| ID | 確認内容 | 期待結果 | 結果 |
|---|---|---|---|
| MT-01 | 起動・最大化表示 | ダークテーマで3画面が表示できる | OK |
| MT-02 | スクリーニング条件付き書式 | PER/ROE/配当等のセルが色分け（青/緑/橙/赤） | OK |
| MT-03 | プリセットボタン | 押下で条件パネルと一覧が変化 | OK |
| MT-04 | 横スクロール・列固定 | コード/銘柄名が固定され多数列をスクロール | OK |
| MT-05 | 個別分析カード/グラフ | 指標カード・5チャート・レーダーが表示 | OK |
| MT-06 | バフェットチェック編集 | はい/いいえ変更でスコア・レーダーが更新 | OK |
| MT-07 | メモ保存 | 保存後に再起動しても内容が残る | OK |
| MT-08 | 比較画面 | 銘柄追加で行が増え、比較グラフが描画 | OK |
| MT-09 | CSV 取込 | sample_stocks.csv 取込で該当銘柄の指標が実データに上書き | OK |
| MT-10 | IRリンク | リンクボタンで既定ブラウザが開く | OK |
| MT-11 | 起動時の全銘柄表示 | 起動直後に約3,700銘柄が一覧表示され、右下に擬似指標件数が出る | OK |
| MT-12 | 全銘柄更新(JPX) | ネット接続時にボタン押下で最新一覧へ更新・基準日が変わる | OK(要ネット) |
| MT-13 | プルダウン視認性 | 業種/規模等のComboBoxを開く | 背景が暗色、選択/ホバー項目が反転色で**文字がはっきり見える** | OK |
| MT-14 | 外部データリンク | 個別分析の「リンク」各ボタン押下 | 既定ブラウザで該当銘柄の各サイトが開く | OK |
| MT-15 | run.bat 起動 | run.bat を実行 | ビルド→起動。失敗時も原因表示で待機し**即閉じない** | OK |
| MT-16 | マスタ更新バッチ | `tools\update_master.bat` 実行 | `%AppData%\DStockAnalysis\data_j.xls` が最新化される | OK(要ネット) |
| MT-17 | テンプレ出力→取込 | 「テンプレ出力」→数値記入→「CSV取込」 | 記入列のみ実データ反映、未記入列は維持 | OK |
| MT-18 | ソース一括表示 | `tools\open_sources.bat 7203` | 7203 の各サイトが既定ブラウザで開く | OK |
| MT-19 | 主要指標スクレイプ | `tools\fetch_data.bat --test 7203` | robots順守で取得し PER/PBR/ROE/利回り/時価総額/EPS を表示 | OK(要ネット) |
| MT-20 | スクレイプ→取込 | `fetch_data.bat`→`output\stocks_real.csv`を「CSV取込」 | 取得列のみ実データ反映・スコア再計算 | OK |

> 注：自動結合テストは %AppData% を参照するため、件数固定値に依存しない検証としている。
> 永続化の厳密検証は単体 UT-DS-01〜04（一時ディレクトリ）で担保する。
